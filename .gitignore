node_modules/
.next/
out/
build/

# Impeccable working files: review captures and the local decision-round payloads
.impeccable/review/
.impeccable/round-out.txt
.impeccable/server.log
.impeccable/*.mjs

npm-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db
.env*.local
!.env.example

# Vercel project link (contains project + org ids)
.vercel

# Generated crawl lists — rebuilt from the catalogue with `npm run seo:crawl`.
# Committed deliberately: they are the artefact you upload, and having them in
# the repo makes it obvious when they drift from the live sitemap.
