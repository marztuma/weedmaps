# Editorial fixtures — PLACEHOLDER DATA

Business and brand names in these files are real companies. **Every ratings figure,
review count, price, distance, opening hour, menu item, potency reading and deal in
this directory is invented for UI demonstration and is not real business data.**

Replace with live API data before this is shown to anyone outside the build.

**Catalogue data now lives in Postgres**, not here. Categories, subcategories,
brands, delivery services and products are seeded by `db/seed.js` into the Neon
project. Only site chrome (`site.json`) and editorial copy (`learn.json`) remain
as JSON. The same placeholder warning applies to the seeded rows.
